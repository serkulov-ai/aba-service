# Скопируйте этот файл в .env.local и впишите значения.
# .env.local в git не попадает (см. .gitignore).

# Supabase — Project Settings → API
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=

# Claude (Anthropic) — console.anthropic.com → API Keys. Используется только на сервере.
ANTHROPIC_API_KEY=
